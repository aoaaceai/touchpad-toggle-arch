pkgname=touchpad-toggle
pkgver=0.0.1
pkgrel=1
pkgdesc="script for switching the touchpad on and off"
arch=('any')
depends=('xorg-xinput')
source=()
license=('BSD')
sha256sums=()

package() {
	install -Dm755 ../touchpad-toggle "$pkgdir/usr/bin/touchpad-toggle"
}
